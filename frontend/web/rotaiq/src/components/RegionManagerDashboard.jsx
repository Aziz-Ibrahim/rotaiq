import React from 'react';
import { Container, Title, Text } from '@mantine/core';

const RegionManagerDashboard = () => (
    <Container>
        <Title order={2}>Region Manager Dashboard</Title>
        <Text>Welcome to your regional control panel. You have access to all branches and employees within your assigned region.</Text>
    </Container>
);

export default RegionManagerDashboard;
